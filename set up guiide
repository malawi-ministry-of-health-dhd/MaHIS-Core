# Build the image
docker build -t my-rails-app .

# Run the container
docker run -p 3000:3000 -e DATABASE_HOST=your-db-host my-rails-app

# Start all services
docker-compose up

# Or run in detached mode
docker-compose up -d

# View logs
docker-compose logs -f

# Stop all services
docker-compose down